name: Build and Test AVP

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore NuGet packages
      run: dotnet restore AVP.sln

    - name: Build Solution (Release configuration)
      run: dotnet build AVP.sln --configuration Release --no-restore

    - name: Run Unit Tests (if applicable)
      run: dotnet test AVP.sln --no-build --configuration Release

    - name: Publish Artifacts
      run: dotnet publish AVP/AVP.csproj -c Release -r win-x64 --self-contained false -o ./publish

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: AVP-Windows-Release
        path: ./publish
